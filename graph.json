async function getGitHubData(Nemets87, Nemets87) {
    const response = await fetch(`https://api.github.com/repos/${username}/${repoName}`);
    const data = await response.json();
    
    // Получаем список файлов в репозитории
    const filesResponse = await fetch(`https://api.github.com/repos/${username}/${repoName}/contents/`);
    const filesData = await filesResponse.json();

    return { ...data, files: filesData };
}

function visualize(data) {
    // ... (остальной код функции visualize остается без изменений)
}

async function main() {
    const username = 'Nemets87';
    const repoName = 'Nemets87'; // Замените на актуальное имя вашего репозитория
    
    const data = await getGitHubData(username, repoName);
    
    visualize(data);
}

main();
